<%= simple_form_for [ reserevation, review ], url: reservations_reviews_path(reservation), method: :post do |form| %>
  <%= form.input :content, as: :text %>
  <%= form.input :rating%>
  <%= form.button :submit %>
<% end %>
